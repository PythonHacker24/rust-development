fn digit(x: i32) -> Vec<i32> {
    let mut digits: Vec<i32> = Vec::new();
    let num = x.to_string();
    let mut length: i32 = num.chars().count();

    let mut multiplier: i32 = 1;

}

fn main() {
    // Code to be implemented
    // work on this
}
